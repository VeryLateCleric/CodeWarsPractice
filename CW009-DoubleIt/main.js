function doubleInteger(i) {
    let doubleI = i * 2
    // i will be an integer. Double it and return it.
    return doubleI;
  }
// I wrote this so fast that I haven't even commited the initial prompt set up.
// Good job me. Go have some ice cream or something