function evenOrOdd(number) {
    return (number % 2 === 0) ? "Even" : "Odd"
}
// In this we use % to determing if the remainder of number / 2 is = 0
// If this equals true, we return Even and if false we return Odd.
